name: Build ZMK

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    secrets: inherit

  dts:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Explain
        run: |
          echo "This job cannot see the build directory from the reusable workflow job."
          echo "To dump zephyr.dts, upload it as an artifact inside the build job, or run a separate debug build here."
